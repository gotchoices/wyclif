TODO:

X- Copy applicable code back from mychips/bin/mychips.js (database SSL, etc.)
X- Wyclif launches its own SPA server
X- Move bin/makecert back to this package
- Test: what if os.hostname() in lib/credentials.js returns null
- Generic access to wyselib basic schema
- Access to wysegi?
- 
- 
